grafana:
  image: pgentile/grafana
  links:
    - graphite
    - elasticsearch
  ports:
    - "80:8080"

graphite:
  image: pgentile/graphite-api

elasticsearch:
  image: pgentile/elasticsearch
  command: -Des.cluster.name=grafana -Des.node.name=grafana -Des.index.number_of_replicas=0
